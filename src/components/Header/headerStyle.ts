import styled from 'styled-components';

export const HeaderStyle = styled.section`

   padding: 2rem 0;
   width: 100%;
   max-width: 170.5rem;
   margin: 0 auto;

   display: flex;
   justify-content: center;
   .logo {
      object-fit: contain;
   }

`;
